{
  "servers": {
    "shadcn": {
      "command": "npx",
      "args": [
        "shadcn@latest",
        "mcp"
      ]
    },
    // ── LOCAL DEV (repo clone) ───────────────────────────────────────────────
    // Runs the locally compiled dist. Switch to global install once published:
    //   npm install -g @h2bis/mcp
    //   then change "command" to "h2bis-mcp" and remove "args".
    //
    // ── GLOBAL INSTALL (end-user) ────────────────────────────────────────────
    // "h2bis-pb-mcp": {
    //   "command": "npx",
    //   "args": ["-y", "h2bis-mcp@latest"],
    //   "env": {
    //     "API_BASE_URL": "https://your-cloud-api.example.com"
    //   }
    // }
    // ─────────────────────────────────────────────────────────────────────────
    "h2bis-pb-mcp": {
      "command": "node",
      "args": [
        "c:/My_work/RnD/h2bis-pb/h2bis-pb-mcp/dist/index.js"
      ],
      "env": {
        "API_BASE_URL": "http://localhost:4000"
      }
    }
  }
}
