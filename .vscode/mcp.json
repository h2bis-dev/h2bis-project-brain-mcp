{
  "servers": {
    "shadcn": {
      "command": "npx",
      "args": [
        "shadcn@latest",
        "mcp"
      ]
    },
    // All runtime config (API_BASE_URL, API_KEY, MONGODB_URI, etc.) is owned
    // by h2bis-pb-mcp/.env — do not duplicate here.
    "h2bis-pb-mcp": {
      "command": "node",
      "args": [
        "c:/My_work/RnD/h2bis-pb/h2bis-pb-mcp/dist/index.js"
      ]
    }
  }
}
